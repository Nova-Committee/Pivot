{
  "schemaVersion": 1,
  "id": "pivot",
  "version": "${version}",

  "name": "Pivot",
  "description": "___________________________________________",
  "authors": [
    "MikhailTapio"
  ],

  "license": "AGPL v3 (Which is invalid because of requiring Minecraft as a hard dependency, but Tapio just won't listen to me)",

  "environment": "*",
  "mixins": [
    "pivot.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.14.6",
    "fabric": "*",
    "minecraft": "~1.19",
    "java": ">=17"
  }
}
